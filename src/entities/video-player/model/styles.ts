import styled from "styled-components";

export const StyledWrap = styled("div")(() => ({
  display: "block",
  width: "100%",
  maxWidth: "800px",
  video: {
    display: "block",
    maxWidth: "100%",
  },
}));
